// DB_URL is the environment variable that holds the database connection string
export const DB_URL = 'DB_URL';

// API_PORT is the environment variable that holds the port number for the API
export const API_PORT = 'API_PORT';
